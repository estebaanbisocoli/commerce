import React from 'react';
import styled from 'styled-components';

const LocalToolbar = styled.div`
  background-color: blue;
`;
const Toolbar = () => {
  return <LocalToolbar />;
};

export default Toolbar;
